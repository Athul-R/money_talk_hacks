"""The narrator. It never does arithmetic — the engine hands it finished
evidence objects and it produces sentences, each tagged with what kind of claim
it is. Without an API key the templated draft ships as-is."""
